# Sky Intake — Cursor skill

Use ao conduzir sessão Sky-Forge em `.sky/sessions/`.

1. `./scripts/sky/sky.ps1 intake -Slug X`
2. INTAKE_PROTOCOL + SKY_MERIT_INDICES
3. Níveis: negócio → produto → **elevation_ux** → técnico → sustentação
4. `sky-elevate` quando product maduro o suficiente
5. Persistir YAML cada turno
