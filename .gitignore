# Session outputs
.sky/sessions/*
!templates/sessions/

# Agent audit & observability (local)
.sky/audit/*
!.sky/audit/.gitkeep
.sky/observability/*
!.sky/observability/.gitkeep

# Intake source archives (large — local only)
.intake-sources/

outputs/*
!outputs/.gitkeep

# ZIPs de pacote gerados para download local no showcase (meta em showcase/registry/*.package.json)
apps/showcase/public/packages/*.zip

# RAG index (PR 4)
.sky/rag/index/

# OS / IDE
.DS_Store
Thumbs.db
.idea/
*.swp
*.swo

# Secrets
.env
.env.*
!.env.example

# Node/Python artifacts (future tooling)
node_modules/
apps/showcase/dist/
__pycache__/
.venv/
dist/

# Local scratch / generated previews
.tmp/

harness-report.json
