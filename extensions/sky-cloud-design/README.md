# Sky Cloud Design — extensão proprietária Sky-Forge

Export handoff `.dc.html` (formato `<x-dc>`).

```powershell
./extensions/sky-cloud-design/scripts/export-dc.ps1 -Slug <slug>
```

Licença proprietária — [LICENSE](LICENSE).
