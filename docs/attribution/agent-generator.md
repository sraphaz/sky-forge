# Atribuição — agent-generator (Matrix Builder)

**Upstream:** https://github.com/ruslanmv/agent-generator  
**Licença:** Apache-2.0

## Padrões adotados

- Contrato antes do código (blueprint + validação)
- Allowed-scope / fail-closed validation → `validate-package.ps1`
- Commits versionados de mudanças aceitas → futuro em `.forge/sessions/{slug}/changelog.yaml`

Nenhum código do SDK `mb` copiado no PR 1.
